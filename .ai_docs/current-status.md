# Current Status — OpenStrata

**Version:** v0.3.25
**Last Updated:** 2026-09-21
**Domain:** openstrata.giveabit.io

## Recent Milestones
- **The tour came back within reach, and everything got a shortcut (v0.3.25, 2026-09-21):** **(1) Tour diagnosis + replay** — Cam reported the popup explainer with the video missing; fresh-browser checks on production proved the card works, so the real bugs were reachability: `chooseStart('setup')` navigated to the wizard before the tour could show, and `openstrata-tour-seen` was a one-way door. The dashboard welcome row now carries a replay pill (`tourReplay`, ×9 locales) that sets `showTour = true` — Tour.svelte needed zero changes, and a replay's dismissal harmlessly rewrites the same flag. **(2) Saved searches** — `src/lib/saved-searches.ts` (pure, 6 tests) pins queries device-local under `openstrata-saved-searches` (cap 8, case-insensitive dedupe, hostile storage degrades to empty); a pin button in ⌘K and a one-tap `/search?q=` strip on the dashboard under the task list. **(3) Group counts in ⌘K chips** — each chip shows its index size, computed from the same index the search runs. **(4) Modal footer hints** — "↵ open · esc close" kbd row (`searchHintOpen`/`searchHintClose` ×9). **(5) Task list filters** — All · Overdue · This week · Done chips; the week/overdue slices read the *full* list (never the collapsed view's parked rows); Done shows ticked setup steps, untick to restore. **(6) Keyboard-first wizard** — number keys jump steps via `svelte:window`, skipped while typing in INPUT/TEXTAREA/SELECT/contentEditable, forward jumps across step 2 enforce the building-name rule like the Next button. **(7) 404 rescue** — `+error.svelte` turns a dead URL's words into up to six `/search?q=` chips (stop-word filtered). **(8) Docs cross-link checker** — `src/lib/docs-links.test.ts` (3 tests) walks every markdown link in `docs/` and `llms.txt` against routes walked from `src/routes` (the filesystem is the authority; dynamic `[category].xml` feeds match any one segment); first run caught 4 broken `../diligence/` links (fixed to `diligence/`). **(9) Back-to-top** — `BackToTop.svelte` mounted in `+layout.svelte`, appears after ~900px, reduced-motion aware, hidden on print, above the mobile dock. New keys ×9 via `scripts/inject-v25-i18n.mjs` + `inject-search-save-i18n.mjs`. Verified: `check` 0/0, **238 tests** (was 229), `audit:i18n` **976 keys × 9 locales**, 0 hard-coded warnings, build green, changelog regenerated (27 releases, 176 items).
- **Search that remembers, narrows, and travels + copy debt to zero (v0.3.24, 2026-09-20):** **(1) Recent searches** — `src/lib/recent-searches.ts` (pure, 7 tests) keeps the last five ⌘K queries device-local under `openstrata-recent-searches` (80-char cap, case-insensitive dedupe, one-tap remove, hostile storage degrades to empty); the modal reads them on open and writes on choose/Enter. **(2) Scoping chips** — ten one-tap filters above the results; `scopeIndex()` filters to one group *before* ranking so queries that missed the global top-12 still surface in-group; short labels from `searchGroupShort(t)` (full names stay on the eyebrows), chips render from `groupsInIndex(index)` so a future group cannot grow a dead chip. **(3) `/search?q=…`** — shareable page running the same `buildSearchIndex` (results can never disagree with the modal); every modal state links to it, Enter-with-no-selection navigates there; in sitemap + llms.txt, page sends `noindex, follow`. **(4) Copy debt 24 → 0** — thank-you flow prose moved to `src/lib/thankyou.ts` as canonical English (documents/custody rule) with chrome through the catalog; home proof band, design specimens, privacy/terms metas, wizard "What happens next →", footer Privacy/Terms all catalogued ×9 locales (968 keys). Audit quirk: a value ending `:'` makes the key regex see a phantom key — `thanksNoHiddenSteps` now ends with an em dash. **(5) Help deep-links** — sidebar help card + HostConnect strip go to `/docs/manual/getting-started` (`helpStartHere`). New guards: every indexed href must resolve to a canonical route (sitemap list, not nav — `/custody` is deliberately outside the bar; the guard caught that distinction on first run), short chip labels never collide per locale, share hrefs round-trip. Verified: `check` 0/0, **229 tests** (was 217), `audit:i18n` **968 keys**, 0 hard-coded warnings, build green, changelog regenerated (27 releases, 176 items).
- **The ⌘K hint caught up with the index (v0.3.23, 2026-09-20):** the empty search modal's copy still said “Search across pages, posts, FAQ, templates, and legal sources” — written when the index had five groups, stale since v0.3.22 grew it to ten (found during the v0.3.22 live verification and queued). The hint now names every group the index serves — pages, posts, FAQ, templates, print-ready documents, the manual, legal sources, primary and official sources, the Strata Tool, and what needs doing — in **all nine locales**. Structural clean-up in the same pass: the modal's inline group-label map moved into `src/lib/search.ts` as `searchGroupLabels(t)` so eyebrows, hint and index share one home, and two labels (`primarySources`, `strataTool`) that had silently fallen back to English in the other eight locales are now overridden everywhere (“Strata Tool” stays untranslated in every locale, matching the file's own convention). Drift guards, because that is exactly how this went stale: `search.test.ts` gained 3 tests — the canonical English hint must contain every label `searchGroupLabels` returns for the groups `buildSearchIndex` actually emits (English is written uninflected, so containment works; an eleventh group fails the suite until the hint is rewritten), no locale may carry the old English sentence or the bare English string, and every locale must resolve ten non-empty group labels. Verified: `check` 0/0, **217 tests** (was 214), `audit:i18n` **921 keys × 9 locales**, build green, changelog regenerated (26 releases, 168 items).
- **Work-protection UX: wizard draft resume, full-site ⌘K, and an honest demo banner (v0.3.22, 2026-09-19):** Cam's three requested improvements, all aimed at not losing work a visitor has already done. **(1) The wizard keeps a draft.** The 8-step Building Template Wizard held everything in component state, so stopping halfway meant coming back to a blank form. `src/lib/resume.ts` (pure, **8 tests**) persists fields + step to localStorage under `openstrata-wizard-draft` on every step change (`persistDraft()` on next/prev/goToStep), `ResumeChip.svelte` offers one-click resume on the dashboard (*“Harbour House — 4 of 8 · in progress”*, `shouldResume()` requires a name + step > 0), the wizard restores fields before prefill and shows a “Draft restored” notice (`resumedFromDraft`), and `generateConfig()` calls `clearWizardDraft()` so a finished build never offers to resume. Parse degrades to null on hostile storage; a nameless step-0 draft is not worth resuming. **(2) ⌘K reaches everything.** `buildSearchIndex` grew two groups — `documents` (all four `printDocuments`, deep-linked `/documents?doc=<slug>`, so **Form B / Form F are reachable by name for the first time**) and `manual` (the eight `manualSections`) — plus a `tasks` entry for the dashboard's “What needs doing” list; seven groups became ten, `SearchModal.svelte` labels them, +3 tests. **(3) The honest demo banner.** `src/lib/demo-banner.ts` (pure, **7 tests**) + `DemoBanner.svelte`: on a sample-data dashboard it states nothing typed is saved to an account, offers **Save my building** (→ wizard), and dismisses for 7 days via a UTC day-stamp (`openstrata-demo-banner-dismissed`). The tested rule is the honesty guarantee: **never** while a session exists, **never** before the auth store settles, **never** when a host is configured — so a signed-in council can never be told their real books are sample data. 12 new catalog keys × 9 locales via `scripts/inject-resume-demo-i18n.mjs`. Verified: `check` 0/0, **214 tests** (was 196), `audit:i18n` **921 keys**, `audit:contrast` **116 pairs**, build green, changelog regenerated (25 releases, 165 items).
- **A simplification pass: four nav items, a first-visit question, and one task list (v0.3.21, 2026-09-18):** Cam asked for a simpler, easier-to-navigate UI; all three changes remove something. **(1) The header is four things instead of six.** Compliance and Docs left the bar for the `Library` menu (beside legal, templates and print-ready documents), so the bar is Dashboard · Strata Tool · Library ▾ · Company ▾. Nothing became unreachable — both are still one click, in the footer and in site search. `src/lib/nav.ts` is still the one authoring home and `navItems` is still derived. **(2) The first visit is a question, not a wall.** A brand-new visitor used to land on a full dashboard of someone else's numbers. `src/lib/components/StartChoice.svelte` asks one question with three ways in (look around / set up a building / sign in); `src/lib/start.ts` owns the rule, the `openstrata-start` key and the `start-pending` class; the inline script in `src/app.html` sets that class **before the first paint** (the same trick the theme switch uses, so no flash) and two `!important` rules in `src/app.css` swap which of the two is visible — CSS rather than a conditional render, so the prerendered markup still contains the whole dashboard for a crawler. The choice is device-local; `rememberStartChoice` + `clearStartPending` reveal the dashboard in place. **The first-run tour now waits its turn**: it is gated on `!startPending`, and answering the question opens it over the dashboard the visitor just asked to see. `src/lib/start.test.ts` (7 tests) asserts the three-file contract, because a rename in any one of them would silently show a first-timer the dashboard. **(3) Four dashboard panels became one ordered list.** `SetupChecklist.svelte` is deleted; `src/lib/tasks.ts` (pure, **16 tests**) merges the setup steps and the statutory deadlines into one list ordered overdue → urgent → soon → routine → **setup last**, rendered by `TaskList.svelte` (tick in place, hide/restore, count of rows left, demo-labelled when signed out). Two honesty rules are encoded and tested: a deadline inside 14 days is urgent *whatever severity the API sent*, and setup steps never interleave with real deadlines. `DeadlinesPanel.svelte` stays on `/tools`. **(4) Fixed: breadcrumbs mislabelled `/documents` as `Docs`** — `'/documents'.startsWith('/docs')` is true and the trail took the first match, so the printable documents page carried a crumb linking to the wrong page. New `navParentFor()` matches on a segment boundary and prefers the deepest href (it also keeps `/docs/manual` → `Docs`). **(5)** The urgency chip was rewritten from amber-text-on-amber-tint to a neutral `surface-3` plate with a coloured dot, because that tint pairing is not covered by `audit:contrast` and “probably 4.5:1” is not a measurement. 23 new catalog keys × 9 locales. Verified: `check` 0/0, frontend **196 tests** (was 164), `audit:i18n` **909 keys**, `audit:contrast` **116 pairs**, build green, and browser-verified — a first visit shows the question with the dashboard hidden and **no tour overlay**, choosing “just looking” reveals the dashboard in place and opens the tour over it, the header renders four items with no Compliance link while the Library menu holds all seven, the merged list renders six rows in urgency order, the breadcrumb on `/documents` reads “Print-ready documents”, and **0 horizontal overflow at 390px and 1440px in light and dark**.
- **One mark everywhere + per-site payment labels + a custody page (v0.3.20, 2026-09-18):** three improvements, plus two real defects found making them. **(1) The pre-rebrand raster logo is retired.** `static/logo.png` and `public/logo.png` are deleted; `/pitch` was the last page shipping the old artwork and now uses the same mark-on-navy plate as the favicon, the PWA icons and the printed letterhead (`BrandMark.svelte` at size 54/64 inside `.mark-lockup`, `background: #102d3b`). Link previews point at a new **1200×630 `static/og.png`** (the old 237×377 logo was upscaled and cropped by every crawler) with `twitter:card` upgraded to `summary_large_image`; `npm run icons` renders it from `icon.svg` plus an SVG text overlay (`openstrata.giveabit.io`, the wordmark, the orange rule). `src/lib/brand-assets.test.ts` (**10 tests**) now guards the whole set: the inline mark must match `icon.svg` path-for-path, the favicon must stay a square simplified rendition, no shipping source may reference the retired logo, `og.png` must be 1200×630, the manifest must point at rasters, and `app.html` must declare the browser-facing icons. **(2) Per-site receive labels.** A rail invoice used to be labelled with the rail's own name (`Lightning Network`) — the same string in every Give A Bit project, so a node operator or a council could not tell whose money had arrived. New `backend/src/rails/receive-label.ts` builds `OST northgate U302 pay-9142` (site code, council, unit, request) as a **pure, deterministic** function of keys the payment request already persists — no migration, no drift between wallet, node and row. 21 tests. An explicit family site-code table (OST / SATO / TAD / MOTO / SHER / STRA / KATO / LIND / CAMD / BTCM / GAB) makes a collision a visible edit; `assertReceiveLabelFor` refuses a foreign label loudly at the rail seam; `quotePayment` now returns `label` + `railName`, `POST /api/v1/payments/quote` returns `receiveLabel`, and the checkout panel shows it (`Payment name` + `checkoutLabelHint`) so the string a council writes on a transfer is the string the node records. **(3) `/custody` — how your money is held.** `0% custody` was four words in a popup; the page now walks the money through three steps (an owner pays → the council holds it → OpenStrata writes it down), states what the software cannot do as **impossibilities** (move money, change a posted entry, act as custodian, see private keys, take a cut), makes the promise testable (verify the chain, export everything, read a Form B, run it yourself), and says plainly what is not live yet. Body prose lives in `src/lib/custody.ts` in English — the same rule `documents.ts` and `manual.ts` follow, because a machine translation of “we never hold your money” is a materially false statement; chrome (12 keys) is in all 9 locales. Linked from the home page's 0% custody proof card (which used to point at `/tools`) and the footer, listed in the sitemap, `llms.txt` and site search; 11 tests including a guard that every catalog key the page references exists. **(4) Fixed on the way:** `static/icon.svg` contained a double hyphen in an XML comment, which is a hard XML error — the rasteriser refused the file, so the master mark had never actually been rendered from its source until `og.png` needed it; and `CHANGELOG.md`'s front-matter had a malformed line (`project: openstrataversion_history:`) that swallowed the project name. Verified: `check` 0/0, frontend **164 tests** (was 143), backend typecheck clean with **214 tests** (was 191), `audit:i18n` **886 keys × 9 locales** (parity green), `audit:contrast` **116 pairs**, build green with `/custody` prerendered; browser-verified on the production preview with the service worker unregistered — `/custody` renders 4 sections / 3 steps / 5 limits / 4 checks / 3 honesty lines at 0 overflow in light and dark, `/pitch` serves **0 raster logos** and 4 vector marks on rgb(16,45,59), `/og.png` 200 and `/logo.png` **404**, and the checkout shows `OST demo U101 demo` in both themes.
- **The intro video's idle frame is a poster (v0.3.19, 2026-09-18):** Kimi delivered a Cam-approved 1920×1080 thumbnail (`static/video/openstrata-intro-poster.png`, “Govern yourself.”) and the greeter popup's `<video>` now sets `poster=` to it (see `VIDEO_POSTER` in `src/lib/components/Tour.svelte`). Without it the player idled on a blank first frame, which reads as broken rather than paused. Verified: `check` 0/0, **143 tests**, build emits the poster to `build/video/` (852 kB) beside the video, and the tour's video element resolves the poster URL and shows it before play.
- **The new brand mark, a grouped navigation, and print-ready documents (v0.3.18, 2026-09-18):** three things Cam asked for, plus the dark-mode defect the navigation work exposed. **(1) The new icon is live.** `static/icon.svg` is the master vector (a brush-drawn strata mark: a dab, three stacked layers, a settling drop), `static/favicon.svg` is a bold small-size rendition (the full mark is 283×448 — at 16px its strokes fall under one device pixel and turn to mush), and `scripts/generate-icons.mjs` (`npm run icons`) rasterises `favicon.ico`, `icon-192.png`, `icon-512.png` and `apple-touch-icon.png`. `BrandMark.svelte` draws the same art inline in `currentColor`, replacing the three-CSS-bars-in-an-orange-plate mark in the header, the app sidebar, the footer, the auth card and the error page — so the favicon, the in-app logo and the printed letterhead are finally one piece of artwork. `app.html` declares ico + svg + 192 + apple-touch; `manifest.webmanifest` points at the PNGs; `static/sw.js` cache bumped `openstrata-v1` → `openstrata-v2` so an installed PWA evicts the old shell. **(2) The header navigation is grouped.** Twelve flat links needed ~1490px inside a 1232px bar, so the strip scrolled internally on every laptop and hid half the site behind an invisible scrollbar. Four inline destinations (Dashboard, Strata Tool, Compliance, Docs) plus two menus (`Library`, `Company`) bring the top level to ~500px; `src/lib/nav.ts` is the one authoring home and the flat list the footer and breadcrumbs read is **derived** from it. The desktop bar starts at `xl` (1280px) — below that the grouped drawer and the floating bottom dock carry navigation instead of a bar that clipped its own edges. The mobile drawer mirrors the same grouping and scrolls internally. **(3) `/documents` — print-ready documents.** Meeting notice, minutes, Form B (Information Certificate) and Form F (Certificate of Payment) rendered by `PrintDoc.svelte` as white paper sheets: letterhead with the mark, reference code, meta table, disclosure tables and signature lines, with `@page { size: letter }` and a page break between documents when the whole set prints. Copy lives in `src/lib/documents.ts` with pure helpers (`docReference`, `addDays`, `daysBetween`, `applyNoticeParams`) and **28 tests**. The dashboard's notice builder no longer assembles its own print page in a popup — it hands its date/time/place/agenda to `/documents` through the query string, so there is exactly one printable notice. **(4) Found on the way: dark mode's selected states were invisible.** `bg-brand-50` / `bg-brand-100` are tints used ~25 times as a selected-state plate and neither was remapped for dark mode, so the plate stayed near-white while its label stepped up to brand-200 or slate-800 — about **1.2:1**. Both steps now have dark values (`#113441` / `#16414f`, plus a pair for the green “brokerage” accent), and `audit:contrast` gained a tint-pair section (106 → **116 pairs**) so it cannot come back. Also fixed: `.marketing-mobile-nav` was missing from the print hide list (the phone dock printed across every page foot), the printed sheet kept its screen padding/radius/shadow because the print rules lost to scoped component styles, and a hover-then-click on a grouped menu closed it. Verified: `check` 0/0, **143 tests** (was 115), `audit:i18n` **872 keys**, `audit:contrast` **116 pairs**, build green, and **114 page/theme/viewport combos with 0 overflow and 0 sub-floor text on a brand tint**, plus nav measured at six widths, menu behaviour (hover→open, click→pin, second click→close, Escape→close, outside→close), print-media emulation, a 4-sheet full-set print, and the notice handoff rendering `Notice of Annual General Meeting` with the council's own details.
- **Per-tab hero artwork + an in-app setup checklist + a public changelog, and a real accessibility sweep (v0.3.17, 2026-09-18):** the three UI improvements Cam asked for, plus the contrast debt they exposed. **(1) `HeroArt.svelte`** — six on-brand SVG motifs (modules / chain / scales / ledger / network / signal) inside the shared `.page-hero` band, mounted on 14 pages by `scripts/wire-hero-art.mjs`. Motif chosen by what the section *is*, drawn in `currentColor` only (so it inherits the band's brand tint and flips with the theme), `aria-hidden`, edge-masked, and `display:none` below 900px. **(2) `SetupChecklist.svelte` + `src/lib/setup.ts`** — the dashboard's four-step "finish setting up" panel (add your units → open the two funds → load your bylaws → close your first month) with deep links, a progress meter, dismiss + restore. **localStorage-only**, hostile storage degrades to "nothing done", and the dismissal lives under its own key. 13 unit tests. **(3) Public `/changelog`** — generated from `CHANGELOG.md` by `scripts/generate-changelog.mjs` → `src/lib/changelog.generated.ts` (20 releases with notes, 5 summary-only from the front-matter history, 119 items). Change-type filter, per-release expand/collapse, newest-first timeline, RSS CTA. Wired into nav, dashboard footer, `static/sitemap.xml` and `static/llms.txt`; 7 sync tests. **(4) Contrast: the audit was silently skipping tokens.** `audit-contrast.mjs` skipped any token with no value in `src/app.css` — which hid the entire light-mode slate ramp (those values come from Tailwind, not us) and every semantic status step. An unresolvable token is now a **failure**, the light slate ramp is stated in `src/app.css`, and the audit went from **60 → 106 token pairs**. Measured in a real browser, the previously-unaudited failures were `text-success` **2.18:1**, `text-warning` **1.99:1**, `text-danger` **3.76:1**, `text-bitcoin` **2.30:1**, white on solid `#f7931a` **2.30:1**, and light-mode `text-slate-400` **2.90:1**. Fixed with documented text steps (`--success-text` / `--warning-text` / `--danger-text` / `--bitcoin-text`, `.text-slate-400/500` overrides, light + dark), `--color-danger-solid` (**5.46:1** with white) for solid danger buttons, and `--on-bitcoin` dark ink on bright Bitcoin fills (**6.96:1**). 40 new i18n keys × 9 locales. Verified: `check` 0/0, **115 tests** (was 95), `audit:i18n` **860 keys**, `audit:contrast` **106 pairs**, build green, **32 page/theme browser combos with 0 overflow and 0 failures**, **24 viewport combos at 390/768/1221px with 0 overflow**, and interaction-verified (filter narrows 74→17 entries, expand 74→77, checklist ticks 0%→25% and survives a reload at 25%).
- **Design-system hardening: branded header bands + a real contrast audit + the "start here" journey strip (v0.3.16, 2026-09-18):** three front-end improvement batches Cam asked for. **(1) `.page-hero`** — one branded header band (brand wash + fine drafting grid faded toward the edges + a brand hairline on the top edge), mixed entirely from theme tokens, replacing 14 hand-rolled per-page Tailwind gradients. Five of those ended in `to-white`, so they painted a **bright band straight across dark mode**; that whole class of bug is now impossible. Codemod: `scripts/migrate-page-hero.mjs`. **(2) `npm run audit:contrast`** — `scripts/audit-contrast.mjs` recomputes WCAG 2.2 contrast for **60 text-on-surface token pairs** in both themes from `src/app.css` (reads `@theme`/`:root`/`.dark`, resolves `var()`, honours the `.dark .text-<token>` text-only override convention) and fails on regression. It found **10 real failures**, all fixed: `--faint` was **2.42:1 on white** → 3.6:1 (documented 3:1 floor, decorative micro-labels only); `--muted` was 4.41:1 on white / 3.9:1 on a surface-3 chip → `#5e6b75`; `text-brand-600`/`text-brand-700` were 3.27–3.68:1 light and **2.67–3.29:1 dark** → text steps now ride the ramp (`brand-700`/`brand-800` light, `brand-300`/`brand-200` dark) via new `.text-brand-*` rules that reference palette variables so the green brokerage accent still swaps; **white on `--orange` was 2.77:1** → solid orange fills moved to new `--orange-solid`/`--orange-solid-deep` (**5.02:1**), the coral kept as a glow/accent token; and the brand ramp's 600/700/800 retuned because white on the old `#0891b2` fill was 3.68:1 (**now 5.88:1**). Wired into CI (`frontend verify`) + the deployment checklist + `docs/SECURITY.md`. **(3) `StartHere.svelte`** — a three-leg journey strip (Explore the modules → Configure your building → Register and go live) under the header band on `/tools`, `/tools/wizard`, `/thank-you` and `/docs/manual/getting-started`; visited legs tick off, current leg highlighted, progress meter; **localStorage-only progress** (no account, no network call, consistent with the data-sovereignty pitch). Pure logic in `src/lib/journey.ts` + 10 tests (`journey.test.ts`). 10 new i18n keys across all 9 locales. Verified: `npm run check` 0/0, `npm test` **95 passed** (was 85), `audit:i18n` 820 keys, `audit:contrast` 60 pairs, build green, and a **browser sweep of 17 pages × light/dark = 34 combos with 0 contrast failures and 0 horizontal overflow**, computed with a real WCAG alpha-composite over the live DOM (parses `oklab()` and `color(srgb …)`).
- **Node/host inventory captured for the MVP rail (2026-09-18):** Cam owns an **UMBREL full node still syncing (~63% IBD, ETA weeks)** and THOR (the family VPS running HERMES) already runs a **pruned bitcoind + LND reachable over Tailscale**. Decision recorded in `docs/DEPLOYMENT.md`: **THOR's pruned node is the MVP rail** (broadcast + confirm need no historical rescan), **UMBREL is the correctness backstop** that can answer address-history questions — until its IBD finishes, watch-only xpub imports may show a *partial* history and the UI must say so rather than present it as a complete ledger. Re-pointing at UMBREL later is a config change (`BITCOIN_NODE_URL`) because the seams are address-agnostic. 9 open questions for Kimi in `docs/KIMI-HANDOFF.md`.
- **Intro video LIVE in the greeter popup + CI-breaking i18n gap fixed (v0.3.15, 2026-09-18):** Kimi's 60-second HyperFrames intro is now playing in the first-run popup on `/`. `src/lib/components/Tour.svelte` defines `const VIDEO_SRC = '/video/openstrata-intro.mp4'` (asset: `static/video/openstrata-intro.mp4`, 57s, 1920×1080, H.264+AAC, 5.3 MB) and renders it in a real `<video>` with controls, `playsinline` and `preload="metadata"`; an `onerror` handler falls back to the placeholder card, and clearing `VIDEO_SRC` reverts deliberately. **Step 1 of the tour was rebuilt as a 720px two-column card** — video left; "What you get" (0% custody / BCFSA-aware compliance / Bitcoin proof trail / portable history) and "Where to start" (three numbered steps) right — because the old single-column card was 934px tall and hid 255px of itself, including the Next button, in a 738px viewport; steps 2–4 stay narrow at 400px and carry no video. 9 new catalog keys (`tourFactsTitle`, `tourFact1`–`tourFact4`, `tourHowTitle`, `tourHow1`–`tourHow3`) across all 9 locales, and `tourVideoFallback` rewritten in all 9 (the "coming soon" line is no longer true). **Also fixed a real bug on `main`:** v0.3.14 added the four video keys to English/fr/es/zh/hi/fil but never to **pl/uk/sw**, so `npm run audit:i18n` was **failing** on the locale-parity guard and would have failed the `frontend verify` CI job. Verified: `npm run check` 0/0, `npm test` 85 passed, `audit:i18n` passed (813 keys), build green with the video emitted to `build/video/`; browser-verified on the production preview at 1221×738 / 768×900 / 390×844 — all 4 steps with **0 hidden overflow and 0 horizontal overflow**, video `readyState 4` at 57s.
- **THOR host identified (2026-09-18, reported by Cam):** the family VPS THOR (runs the HERMES agent) already has a **pruned Bitcoin node + LND**. This is the missing Phase 3 host infra. Recorded in `docs/DEPLOYMENT.md` ("Known host — THOR (VPS)") with 7 open questions for Kimi in `docs/KIMI-HANDOFF.md`: LND reachability + macaroon, the prune target and whether an archive node exists (a pruned node cannot rescan full history, so watch-only xpub imports may miss old txs), which chain (must not enable the rail against real funds by accident), Docker/Tailscale/Node 22 on the box, Ollama + `nomic-embed-text` reachability, a stable MagicDNS name for `PUBLIC_API_BASE_URL`, and disk/RAM headroom.
- **Backend: Ziggy PSBT workflow seam — the BIP174 node path is live (2026-09-16):** `backend/src/ziggy/node-broadcast.ts` now implements **both** node seams. Path A `broadcastPsbtWorkflow`: `walletprocesspsbt` (sign) → `finalizepsbt` (extract) → `sendpsbt` → txid — the BIP174 path the council's hardware-wallet signatures feed; it passes `plan.psbtB64` (the signing coordinator's aggregated PSBT) straight through when set, otherwise serializes a deterministic BIP174-shaped skeleton from the plan (global unsigned-tx map + per-input partial-sig entries, validated by a minimal BIP174 map parser in tests), and it **never fabricates** — it throws when the wallet is missing or `finalizepsbt` reports `complete: false`. Path B `broadcastRawTx` stays the watch-only fallback (`sendrawtransaction`, deterministic placeholder txid when offline — now *documented + tested* as the offline behavior; the old test asserting a throw was stale since the placeholder-txid decision landed). The broadcast endpoint (`POST /api/v1/treasury/psbt/broadcast`) tries the workflow first, falls back to the raw seam, and no longer echoes the "no node client configured" stub reason once a real-looking txid exists. Also fixed a pre-existing typecheck error in `rosa/ingest-vector.ts` (unparenthesized `??`/`||` mix). Verified: typecheck clean, backend **187 tests** (was 182; bitcoin-modules 17→22).
- **Landing page polish (2026-09-15, `dee6bb3`):** restructured the bottom half of the dashboard home (`src/routes/+page.svelte` + `src/app.css`, +206/−36) so it reads as one composition: welcome-row CTA sized to the h1, metric grid with shared chrome and a warm hero card, compact right-rail feed with honest live/demo labeling, building cards with clearer health chips, action cards with tinted glyphs and hover arrows, and a new **proof band** — 3 closing cards (compliance, 0% custody, statutory clocks) with orange CTAs to /compliance, /tools, /docs, single-column below 1050px. Contrast pass: card labels/body checked in light + dark, hardcoded hexes dropped for theme tokens. Verified: svelte-check 0/0, build clean, screenshots at 1280/1100/1050/960/760; 85 tests green at push time.
- **Backend: Rosa pgvector/Ollama retriever + Ziggy PSBT broadcast seam + Tailscale self-host docs (2026-09-16):** Rosa's `Retriever` seam upgraded from the keyword fallback to a real pgvector + Ollama retriever (`backend/src/rosa/vector-retriever.ts`) — embeds the question with `OLLAMA_EMBED_MODEL` (nomic-embed-text, 768 dim) via Ollama `/api/embeddings`, cosine-nearest-neighbor searches the `corpus_chunk` table (migration `0002`, `vector(768)`, HNSW index) via pgvector `<=>`, maps rows back to `SourceRecord`/`RetrievedChunk`, and falls back to `keywordRetriever` when pgvector is unavailable, the table is empty, or Ollama is down — same `Retriever` contract, same `composeAnswer` strictness (citations only, fail-closed), `POST /api/v1/rosa/query` unchanged. Ziggy's PSBT execution seam completed: `backend/src/ziggy/broadcast.ts` adds `broadcastPsbt` (marks a ready plan broadcasted; refuses below threshold) + `postSpendToLedger` (debits the authorized fund on the trust ledger so the on-chain leg reconciles into the same hash chain used for e-transfers/rail quotes/billing), wired as `POST /api/v1/treasury/psbt/broadcast` (treasurer+) in `server.ts` — ready plans broadcast with a `null` txid until a real node client is plugged in; `POST /api/v1/treasury/psbt/plan` unchanged. Deployment rewritten for Tailscale-first, per-user-tailnet self-hosting: any operator brings their own Tailscale (the host joins the operator's tailnet; API reachable at the host's MagicDNS name; Postgres never public; `db.ports`/`api.ports` are `127.0.0.1:...` local-dev mappings to remove on the host); `.env.example` documents Ollama-on-a-different-tailnet-host via MagicDNS, tailnet ACL hardening, and `PUBLIC_API_BASE_URL` pointing at the MagicDNS name. Verified: `backend typecheck` clean, backend 182 tests (bitcoin-modules 15→17; new `planSummary` + `broadcastRawTx` seam-contract test that asserts the seam throws on an unreachable node — the real end-to-end is on the host).
- **Manual docs rebuilt + de-branding (2026-09-15):** `/docs/manual` hub + welcome + getting-started rebuilt clean after the 3 svelte-check errors (dead `t` helper indexing the derived i18n store), dead hub links removed, `src/lib/manual.ts` single authoring home, honesty pass on unsourced pricing claims; "Hermes" removed from all public surfaces (`b1fa2f7`, `f9121b1`).
- **Phase 2 reconciliation audit + Rosa/Ziggy handoff sweep (2026-09-14):** verified the pure `src/lib/reconcile.ts` engine, the interactive `ETransferReconciler.svelte` on `/tools`, live-unit wiring, CSV import seam, and the backend decision endpoint `POST /api/v1/treasury/reconcile`. The prototype is reviewable and never guesses, but it is not yet a production bank-feed or automatic ledger-posting integration. Added explicit continuation notes for Rosa (keyword fallback; pgvector/Ollama pending) and Ziggy (CRF/PO authorization gate real; PSBT/multisig execution and broadcast pending).
- **Design-system release (v0.3.9) — one card language + real dark-mode fix:** extracted a reusable `Card` component (`src/lib/components/Card.svelte`, content/compact/hero variants, `$$restProps` forwarding) and migrated **47 content cards across 9 route pages** (about, blog, docs, legal, templates, compliance, pitch, roadmap, spec, tools, rss); structural cards (tables, accordions) stay `glass-card`. Normalized the marketing type scale (h1 36 / section h2 24 / sub-section 20, card titles weight-800). Sidebar shell pinned to the viewport (footer button no longer falls below the fold — HostConnect strip moved into the dashboard's main column). Dashboard rail + marketing cards both unified on `.panel`/`glass-card` tokens (24px padding, 800-weight headings, shared shadow). **Fixed a site-wide Tailwind v4 cascade bug:** the unlayered element resets (`a{color:inherit}`, `button{border:0}`) were beating the layered `.text-*`/`.border-*` utilities, so every brand link rendered plain ink (invisible dark-on-dark) and buttons lost size/borders — moved the resets into `@layer base`; also wired `:root` color/background to `var(--ink)`/`var(--canvas)` so dark-mode body text actually flips, and lightened `text-bc-blue` in dark mode only (2.35→9.0:1). Verified in a real browser: brand links teal in both modes, dark body `#e2e8f0`, bc-blue chips 6.9–9.0:1, **zero overflow on all 13 pages × 3 sizes** light + dark. Frontend **85 tests**, svelte-check 0/0, build clean
- **Greeter popup card with video section + Kimi intro-video handoff (v0.3.14, 2026-09-17):** the signed-out first-run popup on `/` is now a greeting card with a video section inside the same card (`src/lib/components/Tour.svelte`). New visitors get the short hello + one-line explanation and the 30–60s intro in one glance; the video section is a tease with a CTA while the asset is absent, and a real `<video>` path is wired via a single `VIDEO_SRC_PLACEHOLDER` string swap. New i18n keys `tourVideoTitle/Sub/Cta/Fallback` (English for now). Kimi handoff added: `docs/KIMI-HANDOFF.md` session section + `docs/VIDEO-SPECS.md` (specs + 1-minute script + delivery checklist). Kimi owns the recording (HyperFrames, her own images/video, young English woman voice) and the asset URL; she can post to `static/video/` herself or send the URL back for the frontend swap. Version bumped to v0.3.14 everywhere. Verified: `npm run check` clean; `npm test` green (85 tests); popup renders on `/` for signed-out fresh visitors; video section inside the greeting card, inline, does not break the 4-step tour.
- **Next 20 shipped (v0.3.8) — live flows, governance, Bitcoin & trust:** the tools page is now a full operations desk. **Live flows** — RosaChat (citation-only compliance Q&A, `POST /rosa/query` live / honest demo corpus), register-your-building wizard ending in real `POST /auth/register`, QR scan-to-pay with wallet deep links (`qrcode`, demo-quote fallback), Forms B/F 7-day statutory tracker, MyUnit member panel. **Governance** — BallotEngine (resolution + roll-call tally, 4 thresholds, minutes export via `/meetings/vote`), CRT-ready BylawCaseFile, MeetingNotice with statutory advance-window check, compliance HealthScore (auditable formula from deadlines + AR). **Bitcoin** — MempoolBalances (watch-only sats from mempool.space), DcaPlanner (allocation/frequency/horizon + Form B disclosure % via `/treasury/dca/plan`), RateSparkline (live CAD/BTC + as-of), RailsReadiness checklist (LND/LNBits/Liquid/PayNym/Nostr envs). **Trust/polish** — ChainViz ledger hash-chain rail with re-verify, PwaChrome (offline banner + install chip), a11y (0 warnings), illustration scenes in the 4-step tour + empty states, HostConnect demo→live strip. **Verified in a real browser:** zero horizontal overflow 390→1280px on home + tools, Rosa answers with citations, ballots tally, QR renders, tour completes + dismisses forever. Frontend **78 tests** (+8), i18n **893 keys × 9 locales** (+156), svelte-check 0/0, backend 173 tests
- **All 20 user-flow/GUI/Bitcoin improvements shipped (v0.3.7):** backend gained migration `0006_council_members` + `PostgresMemberStore`/`MemMemberStore` (tenant-scoped `/members` CRUD, seeded from unit owners at register), a verified `GET /ledger/entries` (hash-chain explorer) via new `LedgerEngine.entries()`, and `GET /deadlines` (statutory calendar + open payment quotes, sorted urgent-first). Frontend ships 14 new components: **user flow** — CheckoutFlow (pay-fees quote → rail picker → confirm → receipt with sats/locked rate/Satohash stamp), MonthlyClose (billing run → late notices → ledger), BylawCase (complaint → notice → 14-day BLOCK_FINE_ACTIONS lock → fine/no-fine), MemberWorkspace (owners per lot), DeadlinesPanel ("What's due" task center); **GUI/design** — brand-accent theming (orange ↔ BC-green brokerage via `data-accent` palette + topbar toggle), `/design` system page, inline SVG illustration set, print stylesheet, glossary explainers; **Bitcoin** — RailsStatus (enabled rails + live CAD/BTC as-of), SigningRoom (PSBT signature progress + broadcast), WalletPanel (xpub + per-unit addresses with copy/mempool links), receipts in checkout; **trust/data** — LedgerExplorer (verified chain + CSV export), ExportCenter (portable JSON, CRT bundle, Form B/F, ledger CSV), MemberManager (admin invites + temp passwords), NotificationsFeed (bell from real deadlines), RateBadge. New API helper modules (payments, billing, bylaw, members, deadlines, ledger entries, admin users) with +10 frontend tests (**70 total**); i18n **680 keys × 9 locales** (+92); **zero horizontal overflow at 390→1698px on home + tools** (fixed grid min-width blowout + tablet topbar overflow), accent toggle + glossary + bell verified in a real browser. Backend **173 tests / 18 files**
- **All 20 GUI & user-flow improvements shipped (v0.3.6):** four batched commits (`eac768f`→`584dbb4`, pushed). **Foundation:** one SVG icon system replacing emoji/unicode glyphs; **`.glass-card` was referenced on 60+ marketing cards but never defined — those cards were rendering as bare text; now a real card with shared `--radius-card/--shadow-card/--border-card` tokens** (also defined `tooltip-bubble`, previously phantom); typography ramp (one heading voice); designed `EmptyState` component (mark + message + CTA) in dashboard search/notifications/unit payments. **Navigation:** breadcrumbs on every marketing page; tools sub-nav with "Jump to live demos"; scroll-spy `PageToc` on legal/docs/spec/roadmap; the ⌘K palette now indexes the strata-tool module catalog (new `tools` search group); mobile bottom dock on every page. **Alive:** `startViewTransition` fade+slide route transitions (reduced-motion aware); shimmer skeletons (dashboard metrics + unit detail, 2.5s safety timer); dynamic time-of-day greeting + real date + signed-in first name; metric-card sparklines fed by the live `/ledger/series`; hover/active/focus micro-interactions. **Trust:** 4-step first-run tour (localStorage-remembered, signed-out visitors only); error-variant toasts + inline form validation; confirm dialogs for remove-unit + sign-out; "Last synced HH:MM" + refresh chrome on SubAccounts; hero pattern (eyebrow + title + intro + ONE primary action) added to FAQ/Blog/Roadmap/Legal. Bonus fix: marketing header overflowed 20–52px at 768–800px (jurisdiction picker → `lg+` only); **re-verified zero horizontal overflow at 390→1698px in a real browser**, tour overlay + click-through navigation confirmed. Frontend **56 tests** (+11), svelte-check 0/0, i18n **588 keys × 9 locales**, build clean
- **Landing page fixed (v0.3.5):** the home dashboard now renders as a standalone app shell — no marketing header/footer above it (the double-shell caused a ~1570px-min header that overflowed horizontally at every common laptop width → "page slides sideways under the sidebar", and a `100vh` fixed sidebar that sat 105px down so its bottom buttons were clipped below the fold). All dashboard links wired to real pages (sidebar nav, footer columns, mobile nav, "Need a hand", "View all"), theme toggle added to the topbar, marketing header nav scrolls internally, sidebar is `100dvh` with a scrollable nav region. Verified in a real browser at 1698/1440/1280/1024/390px: zero horizontal overflow, sidebar help + footer visible at every size, click-through navigation works
- **Per-council DB-backed units (v0.3.5, #5 — the largest remaining tenancy step):** migration `0005_council_units.sql` (`unit` table, PK `(community_id, unit_ref)`); `UnitStore` interface + `PostgresUnitStore` + `MemUnitStore`; server rewired to store-backed `/units` — `GET /units` tenant-scoped, **`GET /units/:unitRef` unit detail** (AR balance hash-chain verified + payment requests: unit→payment→ledger traceability), `POST /units` (treasurer+, refs canonicalized), `DELETE /units/:unitRef` (admin); **register seeds each council's own building**; payment-quote unitRefs canonicalized; **billing AR fund aligned to the canonical `ar:unit-<n>`**; **fixed a latent `PostgresPaymentRequestStore.markStatus` bug** (`status` was being set to the referenceCode — the e2e re-quote assertion would have failed on real Postgres); frontend Form K hub gains a live unit-detail panel + add/remove unit controls. Backend **167 tests / 16 files** (+12), frontend 45 tests, i18n 565 keys × 9 locales, svelte-check 0/0
- **20-item upgrade push (v0.3.4):** 15 of 20 items shipped as code in 5 batched commits (`3ed66e7`→`89fc0b3`, pushed). Auth rate limiting (#6: failure-counting per email+IP, 429 + retry-after, success clears; `AUTH_RATE_LIMIT_*` config); build-time CSP pinning (#2: `scripts/generate-csp.mjs` in prebuild pins `connect-src` to `CSP_API_ORIGIN`/`PUBLIC_API_BASE_URL`); monthly treasury series (#4: `GET /api/v1/ledger/series`, chain-verified, pitch chart now live); live CAD/BTC provider (#7: `LiveRateProvider` from mempool.space, cached, env fallback, in production entrypoint); MeetingsTool (#8), SubAccounts (#10), CSV bank-feed import (#12); Bitcoin batch (#15/#16/#18/#19: PSBT 3-of-5 orchestration seam + `/treasury/psbt/plan`, xpub import UI + `GET|POST /api/v1/rails/xpub` with per-unit BIP32 paths, `planDca` + `/treasury/dca/plan`, `POST /api/v1/compliance/stamp` hash-of-record); portable export (#20: `GET /api/v1/export/portable`), CRT evidence (#11: print-ready HTML chain bundle), print-ready Form B/F (#9: `GET /api/v1/forms/b|f/:unitId`). Backend **155 tests / 14 files** (+28), frontend 42 tests, i18n 561 keys × 9 locales, svelte-check 0/0. Deferred: #1 live verify, #5 per-council DB units, #13/#14/#17 rails/broadcast/BOLT-12 (host + LND/LNBITS channels later)
- **Landing security + shell tightening (v0.3.3):** fixed the CSP in `static/_headers` that was blocking the page's own assets — Google Fonts (style/font sources added — the Manrope/DM Mono fallback to system fonts was the visible “floating” look), Umami analytics (script/connect sources added), and the live-API origin (`connect-src` now allows `https:`, documented: pin to an explicit host in `_headers` once the API is fixed); added `upgrade-insecure-requests`; removed the duplicate Google Fonts `@import` from `src/app.css` + dropped unused `Inter` from the head link (fonts load once); tightened the dashboard shell — content 1410→1280px, main-grid gap 38→28, tighter card/panel/metric spacing, crisp 2-layer card shadows instead of the soft 12px glow (dark-mode too). Checks green: svelte-check 0/0, 42 tests, i18n 523 keys
- **Frontend wired to the live backend (v0.3.2):** new `src/lib/api/` client — base-URL resolution (`PUBLIC_API_BASE_URL` build-time env or `localStorage['openstrata-api-base']` runtime override, else demo mode), bearer-token persistence (`openstrata-token`), typed fetch wrapper (`apiFetch` with `ApiError`/`ApiUnavailableError`), Svelte auth store + `bootstrap()` session restore, typed ledger/units/rails endpoint helpers; **AuthModal** (sign-in / create-account tabs) in the dashboard topbar with sign-out menu + council name in the workspace switcher; dashboard shows a **Live/Demo pill**, pulls the CRF + operating balances from `/api/v1/ledger/balance`, and the tools units matrix swaps to `GET /api/v1/units` when a session is live (falls back to the demo registry); the **e-transfer reconciler** matches its simulated inbound transfers against the live unit registry when signed in (`apiUnitsToUnitRefs` adapter, LIVE badge; bank-feed ingestion remains Phase 5); the **pitch deck** shows the real CRF balance + CAD/BTC from `/rails/status` when live (simulation walk gated to demo mode, hero badge flips Live ↔ Demo); `.env.example` + `docs/DEPLOYMENT.md` document both exposure paths (Tailnet-only vs public HTTPS behind JWT + CORS); 13 new i18n keys across all 9 locales (523 total, parity audit green); **17 new frontend api client tests**, `svelte-check` 0/0
- **Auth + multi-tenant councils (v0.3.1):** zero-dependency JWT auth (HS256 via node:crypto, scrypt password hashing) with **admin / treasurer / member** role gates; open signup (`POST /api/v1/auth/register` creates a council + first admin), login, `/auth/me`, admin user management with one-time temp passwords; every `/api/v1/*` route except `/health` + the Rosa KB now requires a Bearer token and derives the ledger `community` from the token's `cid` claim (no more client-supplied community); payment quoting/confirming is tenant-scoped (idempotency key now `(community_id, ref_id, unit_ref, rail)` via migration `0004_auth_and_tenancy.sql`); new `backend/src/auth/` module (model, jwt, passwords, store + mem/pg adapters); **backend suite now 127 tests / 11 files** (19 new auth + tenancy tests incl. forged/expired tokens, role gates, cross-council isolation) + **new Postgres e2e smoke suite** (`tests/e2e-smoke.test.ts`, runs against a live `DATABASE_URL`) + **CI `backend-e2e` job** that spins up `pgvector/pgvector:pg17` and runs migrate + the smoke suite on every push
- v0.2.x series: responsive dashboard + shared shell, 9-locale i18n parity (217→509 keys), PWA + dark mode + search, Phase 2 e-transfer auto-reconciliation prototype, Phase 3 backend scaffolding (immutable trust ledger, Rosa RAG, Ziggy treasury), automated fee billing + late notices, CRT-proof bylaw enforcement state machine, sovereign payment rails (Bitcoin on-chain, Lightning/LNURL, Liquid, PayNym BIP-47, Nostr) with quoting + shared reconciliation reference
- **Phase 3 core product completed + hardened (v0.3.0):**
  - **Form B/F + meetings + payment flows** (`0db3ce3`): Form B (information cert) / Form F (payment cert) issuance endpoint, AGM/SGM/council quorum + threshold-voting routes, `POST /api/v1/payments/confirm` that marks a quoted payment paid AND posts it to the unit's AR ledger (reconciles like an e-transfer), idempotent `payments/quote` backed by a persisted `PaymentRequestStore` (`PostgresPaymentRequestStore` + in-memory for tests) keyed on `(refId, unitRef, rail)`, `Idempotency-Key` dedupe on ledger/billing writes, Fastify JSON-schema body validation on write routes
  - **Rails hardening** (`1112968`): real BIP-173 bech32/bech32m checksum verification for `bc1`/`bc1p` (taproot), LNURL, `npub` via `decodeBech32` + `bech32Encode`; pluggable `cadPerBtc` `RateProvider` (env-seedable, static/cached fallback) wired through `/rails/status` + `/payments/quote`; watch-only xpub → deterministic per-unit BIP32 child index (`deriveUnitAddress`/`unitChildIndex`)
  - **Operational CLI + docs** (`f7bdc2d`, `3c17beb`): `npm run cli -- rosa ingest` (validate + probe the BC corpus), `npm run cli -- ziggy simulate` (walk treasury scenarios through the state machine); full `backend/API.md` request/response reference linked from README
  - **Canonical unit/lot master-data model**: new `backend/src/units/` (canonical `UnitRecord` + `UnitRegistry`, unit-ref normalization, deterministic `ar:unit-<n>` AR ledger fund codes, reconciliation keys) exposed as `GET /api/v1/units`; mirrored on the frontend via `$lib/units.ts` so the site's unit matrix and e-transfer reconciliation widget consume one source of truth shared with the backend (backend 108 tests, frontend 25 tests, all green)
  - **API test coverage** (`40db03f`): 13 new route tests — payments/confirm edge cases (unknown ref, schema 400, double-confirm refusal, paid request surfacing through re-quote), Form B 7-day deadline + Form F on a clear unit, AGM/council/rescheduled quorum, majority/unanimous voting rejections; `MemPaymentRequestStore.markStatus` fixed to propagate to both by-ref and by-key indexes. **Backend suite now 99 tests, 9 files, all passing; typecheck clean**

## Known Issues
- **THOR's bitcoind is pruned.** It is fine for the MVP (broadcast, confirm, watching UTXOs from now on) but it **cannot rescan full history**, so watch-only xpub address imports may show an incomplete picture until Cam's UMBREL full node finishes its IBD (~63% as of 2026-09-18) and the rail is re-pointed at it. Do not present pruned-node address history as a complete ledger
- THOR's bitcoind has **no wallet loaded** (`listwallets` returned `[]` on 2026-09-18, verified by Kimi on the box). The PSBT workflow seam (`walletprocesspsbt`/`sendpsbt`) needs one; LND's own wallet does not satisfy it. Cam greenlit `createwallet` on 2026-09-18 — it has not been created yet, and it is step one of deploying the rail
- `/custody` body prose is **English-only by decision** (`src/lib/custody.ts`), matching `documents.ts` and `manual.ts`. Do not machine-translate custody wording: “we never hold your money” mistranslated is a materially false statement. If a reviewed translation exists, move it through the catalog like any other page chrome
- `--faint` sits at a documented **3:1** floor rather than 4.5:1: it drives only decorative 9–10px uppercase eyebrow labels. If it is ever used for text that carries meaning, it must be raised to the 4.5:1 floor and `scripts/audit-contrast.mjs` updated in the same change
- `.page-hero` mixes every colour from theme tokens on purpose. A future header band must not reintroduce a fixed surface (`to-white`, `bg-white`) — that is exactly how five pages got bright bands across dark mode
- `npm run audit:i18n` checks every locale against French parity, so **new catalog keys must land in all 9 locales in the same commit** — v0.3.14 missed pl/uk/sw and left `main` with a failing audit until v0.3.15. Use the `scripts/inject-*-i18n.mjs` pattern and run the audit before pushing
- The popup's step 1 is a fixed two-column breakpoint at 700px; below that it stacks and is tuned to fit a 390×844 phone exactly. Adding more rows to "What you get" or "Where to start" will reintroduce inner scroll on phones
- **The first-visit question is a three-file contract.** `src/lib/start.ts` owns the rule and the key, `src/app.html` sets the class before paint, `src/app.css` acts on it. Renaming the class or the key in one place must be done in all three — `src/lib/start.test.ts` reads all three files and fails if they disagree
- **The wizard draft is written on step moves, not on every keystroke.** `persistDraft()` fires from `nextStep`/`prevStep`/`goToStep` in the wizard — a visitor who types on one step and closes the tab loses that step's edits. Extending it to `$effect`-on-every-change would cover that, at the cost of a localStorage write per keystroke; revisit only with a debounce. The draft is also single-slot (one per device) by design — multiple simultaneous buildings are what **Saved buildings** is for
- **The demo banner's dismissal is a UTC day-stamp with a 7-day TTL** (`openstrata-demo-banner-dismissed`). It is device-local like every other nudge; clearing storage brings it back. `shouldShowDemoBanner` is the single rule — if the banner ever needs new visibility conditions, extend that function and its tests, never the component
- **`TaskList` replaced `SetupChecklist`, but the older storage keys still matter.** `SETUP_KEY` / `SETUP_HIDE_KEY` are still read (a council that dismissed the old checklist must not see a resurrected one) and `TASKS_HIDE_KEY` is the new one. Do not drop the legacy reads without a migration thought
- The Tailwind v4 unlayered-reset cascade bug (`a{color:inherit}`/`button{border:0}` beating utilities) is fixed via `@layer base` — if a future bare-element rule is added outside a layer, utilities will silently lose again; keep element resets inside `@layer base`
- Domain-specific statutory, financial, feed, module, and protocol records remain canonical English/data-driven until reviewed translations are available
- Phase 3 backend is functional but **not yet deployed**: Rosa now has the pgvector/Ollama retriever seam wired (`vectorRetriever` in `index.ts`, keyword fallback as the safe floor) — Rosa keeps running before Ollama is provisioned, and answers with citations either way; Ziggy PSBT execution seam is complete (`broadcastPsbt` + `postSpendToLedger` + `POST /api/v1/treasury/psbt/broadcast` — ready plans broadcast, the debit posts to the trust ledger), with the real node client now implemented as two seams (`broadcastPsbtWorkflow` — walletprocesspsbt → finalizepsbt → sendpsbt — preferred, and `broadcastRawTx` — sendrawtransaction — watch-only fallback); what remains is host infra only: a bitcoind/LND on the tailnet + `BITCOIN_RAIL_ENABLED=true` so the endpoint returns a real txid; the Docker Compose stack needs a real deployment on a host behind Tailscale (the Postgres adapters are now covered by the e2e smoke suite + CI, but a live host run is still pending); `backend/.env` needs a real secrets path on the host (generate a strong `AUTH_SECRET` via `openssl rand -base64 48`); the deployment model now assumes each operator brings their own Tailscale (per-user tailnet, API at the host's MagicDNS name)
- Sovereign rails are **prepared, not connected**: no LND / Liquid daemon / PayNym notifier / Nostr relay is running on a host; `cadPerBtc` needs a live rate feed. Enable + point endpoints via `.env` when daemons exist
- Units are now per-council DB-backed (migration 0005) with unit-detail traceability; Form B/F PDF *file* output remains a browser-side print path (print-ready HTML), not a server-side PDF
- Login has failure-counting rate limiting (429 + retry-after, success clears); invite-only accounts still pending before public exposure
- The live site remains a fully functional front-end product demo; legal/statutory content awaits professional review
- Landing page now a standalone dashboard shell with real links; the marketing header/footer appears on all non-home pages only
- CSP `connect-src` allows `https:` by default for the configurable live-API origin — the prebuild script pins it to an explicit origin when `CSP_API_ORIGIN`/`PUBLIC_API_BASE_URL` is set
- The host deploy + rails daemons (#1/#13/#14) and BOLT-12 (#17) remain the outstanding items — all infra-gated
- The frontend API client is built and documented, but no backend is reachable yet: the host deploy is still pending, and the API-exposure path (Tailnet-only vs public behind auth + CORS) is decided at deploy time via `PUBLIC_API_BASE_URL`
- No live monthly series exists on the backend, so the pitch-page income/expense + rental-index charts stay demo data (explicitly labeled “simulated live ledger”); same for bank-feed e-transfer ingestion (Phase 5)

## Next Steps
- The 20 GUI improvements shipped; per-page dark-mode **computed-contrast** audit is now green (brand links, body text, bc-blue chips) — optional follow-ups: real image assets for empty states/tour, and wiring `LiveSync` refresh into the remaining live widgets (MeetingsTool, reconciler)
- **Deploy on a Tailscale host:** `docker compose up -d`, set `AUTH_SECRET` in `backend/.env`, `npm run migrate`, then run the e2e smoke gate: `DATABASE_URL=… npm run test -- e2e-smoke` (covers `/api/v1/ledger`, `/billing/run`, `/payments/*`, `/forms`, `/meetings/*`, cross-council isolation on the real adapters)
- Deploy a host and set `PUBLIC_API_BASE_URL` at build time (or `localStorage['openstrata-api-base']` at runtime) to flip the dashboard from demo to live; if exposing publicly, add login rate limiting + the CORS allowlist first
- Pick Rosa embedding/chat models + provision Ollama on the host (or a tailnet host), then run `rosa ingest` to index the BC corpus into `corpus_chunk` with real embeddings; the `vectorRetriever` is already wired and will pick it up — `POST /api/v1/rosa/query` stays the same
- Provision the Bitcoin rails' daemons (LND, Liquid node, PayNym notifier, Nostr relays) on the host and enable rails via `.env`; wire a live `cadPerBtc` rate feed; connect `payments/confirm` → real on-chain/LN broadcast
- Move to per-council DB-backed units + unit→payment→form traceability UI
- Professional human review of the machine-drafted locale overrides before they are treated as reviewed