# OpenStrata — Last Updated 2026-08-26 by Grok

Brief: #20 done — the frontend dashboard is wired to the live `/api/v1` backend (v0.3.2). New zero-dep `src/lib/api/` client: base-URL resolution (`PUBLIC_API_BASE_URL` build-time env, `localStorage['openstrata-api-base']` runtime override, else demo mode), bearer-token persistence, typed `apiFetch` wrapper, Svelte auth store with session restore, and typed ledger/units/rails endpoint helpers. New AuthModal (sign-in / create-account tabs, open signup → council + first admin) in the dashboard topbar with a sign-out menu; the header pill flips Live ↔ Demo; the reserve-funds + operating metrics pull `/api/v1/ledger/balance` when signed in; the tools units matrix swaps to `GET /api/v1/units` (LIVE badge). Everything falls back to the existing sample data when no backend is reachable, so the public site never breaks. i18n: 13 new keys across all 9 locales (523, parity audit green); 17 new frontend api client tests; svelte-check 0/0. Both API-exposure paths (Tailnet-only vs public HTTPS behind JWT + CORS) documented in docs/DEPLOYMENT.md + root `.env.example`. Follow-up: the e-transfer reconciler now matches against the live unit registry (`GET /api/v1/units`, `apiUnitsToUnitRefs` adapter, LIVE badge) and the pitch deck shows the real CRF balance + CAD/BTC from `/rails/status` when live — the simulation walk is gated to demo mode and the hero badge flips Live ↔ Demo. Monthly-series charts (income/expense, rental index) and bank-feed e-transfer ingestion stay demo/Phase 5 since no live backend series exists.

Commit: (uncommitted — working tree; follows `697dd26` on main)
