# openstrata — Last Updated 2026-09-16 by Grok (M3)

**Brief:** Ziggy PSBT workflow seam shipped — the BIP174 node path (`walletprocesspsbt → finalizepsbt → sendpsbt`) is live in `node-broadcast.ts` with the raw-tx seam as the watch-only fallback; the broadcast endpoint tries the workflow first and stops echoing the stub reason once a real txid exists. Backend 187 tests (was 182), typecheck clean. All Phase 3 code items are now complete — what remains is host infra (bitcoind/LND on the tailnet + `BITCOIN_RAIL_ENABLED=true`).

**Commits:** this batch on top of `cb6b878` (v0.3.10).

- **What shipped (this batch):**
  - **PSBT workflow seam (Path A)** (`backend/src/ziggy/node-broadcast.ts`): `broadcastPsbtWorkflow(plan, btc) → { txid, hex }` — passes `plan.psbtB64` (the signing coordinator's aggregated PSBT) straight to `walletprocesspsbt` when set, otherwise serializes a deterministic BIP174-shaped skeleton from the plan (global unsigned-tx map + per-input partial-sig entries derived from the plan's signature bookkeeping). **Never fabricates:** throws when the node is unreachable, the wallet is missing, or `finalizepsbt` reports `complete: false` (below threshold). Real txid only from a real node.
  - **Raw-tx seam (Path B) documented as the offline fallback**: `broadcastRawTx` keeps the deterministic placeholder txid (`psbt:<planId>:<shortHash>`) when offline — now *tested* as the documented behavior (the old test asserting a throw was stale since the placeholder decision landed in `a5e3844`).
  - **Endpoint wiring** (`backend/src/api/server.ts`): `/treasury/psbt/broadcast` tries the workflow seam first, falls back to the raw seam on failure, and only leaves `txid` null when both fail; the response `reason` is cleared once a real-looking txid exists (no more "no node client configured" next to a txid).
  - **BIP174 serializer fixes + tests**: fixed a pre-existing typecheck error in `rosa/ingest-vector.ts` (unparenthesized `??`/`||` mix); bitcoin-modules 17→22 tests — BIP174 map-parser validation of the skeleton (magic, global map, per-input 0x02 partial-sig entries, empty output map), aggregated-`psbtB64` pass-through assertion, workflow never-fabricate contract, `sendpsbt` non-txid rejection, offline placeholder determinism, and an endpoint fallback-order test (workflow fails → raw placeholder txid → no stub reason).
- **Docs updated (this batch):** `backend/API.md` (broadcast endpoint: two node seams in order), `docs/DEPLOYMENT.md` + `docs/TAILSCALE-ONBOARDING.md` (workflow seam preferred, raw fallback, cookie file supported by both seams), `docs/WORKPLAN.md` + `docs/ROADMAP.md` (on-chain broadcast plug-in seam complete), `.ai_docs/current-status.md`, `docs/KIMI-HANDOFF.md`, `LATEST-UPDATE.md`.
- **Verified:** `backend npm run typecheck` → clean; `backend npm test` → **187 tests** (was 182; bitcoin-modules 22), e2e smoke 6 skipped (no DB).
- **Decisions:** the workflow seam fails closed (never fabricates a txid) while the raw seam stays the offline-iterable fallback — the endpoint's job is to pick the seam the host can actually run; the coordinator's aggregated `psbtB64` always wins over the skeleton, so hardware-wallet signatures flow through the same RPC chain.
- **Remaining (all host-infra-gated, no open Phase 3 code items):** deploy the stack on a Tailscale host, provision Rosa Ollama + `rosa index`, provision bitcoind/LND + enable rails so `/treasury/psbt/broadcast` returns a real txid.
