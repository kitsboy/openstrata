# openstrata — Last Updated 2026-09-16 by Grok (M3)

**Brief:** Phase 3 backend completion pushed (two batches) — Rosa pgvector/Ollama retriever + corpus → pgvector indexer + Ziggy PSBT broadcast + on-chain reconcile seam + bitcoind raw-tx broadcast plug-in seam + a self-contained Tailscale onboarding doc any operator can follow to bring their own Tailscale; typecheck + backend 182 tests green before push.

**Commits:** `a6f1a71` (code: Rosa indexer + bitcoind raw-tx broadcast seam + CLI + server route + tests) + `1d5fe0f` (docs: Tailscale-first per-user-tailnet self-host model + route/API/DEPLOYMENT/ROADMAP/WORKPLAN/current-status/KIMI-HANDOFF/LATEST-UPDATE).

- **What shipped:**
  - **What shipped (batch 1 — code):**
  - **Rosa corpus → pgvector indexer** (`backend/src/rosa/ingest-vector.ts`): reads the in-memory BC corpus, embeds each document's text with Ollama `/api/embeddings` (`OLLAMA_EMBED_MODEL`, nomic-embed-text, 768 dim), upserts rows into `corpus_chunk` (migration `0002`, idempotent per citation, skips empty-text docs, fails loudly if pgvector/Ollama are unreachable). Operator tool, not a runtime gate — runtime still falls back to keyword. CLI: `rosa ingest` (pure validate, unchanged), `rosa index` (embed + write corpus — needs Ollama + DB), `rosa reset` (dev: drop + re-create `corpus_chunk`).
  - **Ziggy on-chain broadcast plug-in seam** (`backend/src/ziggy/node-broadcast.ts`): bitcoind JSON-RPC raw-tx path first (`sendrawtransaction` → txid; the PSBT workflow seam `walletprocesspsbt → finalizepsbt → sendpsbt` is the next step). `broadcastRawTx(plan, btc, outputs) → { txid, hex }`; `planSummary(plan)` helper for logs + the endpoint response. The `/treasury/psbt/broadcast` endpoint now calls `broadcastRawTx` when `BITCOIN_RAIL_ENABLED=true` + `BITCOIN_NODE_URL` + `BITCOIN_RPC_USER`/`BITCOIN_RPC_PASS` are set and returns the real txid; otherwise txid stays null until a node client is plugged in.
  - **`cli.ts`** gains `rosa index` + `rosa reset`; **`server.ts`** wires the raw-tx seam into `/treasury/psbt/broadcast`; **`bitcoin-modules.test.ts`** gains `planSummary` + `broadcastRawTx` seam-contract test (asserts the seam throws on an unreachable node — the real end-to-end is on the host) alongside the existing broadcast-seam + endpoint tests.
  - Verified: `backend npm run typecheck` → clean; `backend npm test` → 182 tests (was 173): bitcoin-modules 15→17, all other suites unchanged, e2e smoke 6 skipped (no DB). New files: `backend/src/rosa/ingest-vector.ts`, `backend/src/ziggy/node-broadcast.ts`. Modified: `backend/src/cli.ts`, `backend/src/api/server.ts`, `backend/tests/bitcoin-modules.test.ts`.
- **What shipped (batch 2 — docs):**
  - `backend/.env.example`: Tailscale-host-metadata + per-user-tailnet notes + Ollama-on-a-different-tailnet-host via MagicDNS + `BITCOIN_RPC_USER`/`BITCOIN_RPC_PASS` + `CAD_PER_BTC` static override.
  - `backend/README.md`: Deployment model section (self-hosted, Tailscale-first, per-user-tailnet, minimal host setup, Rosa Ollama on a different tailnet host, production hardening) + CLI section (`rosa ingest`/`rosa index`/`rosa reset`/`ziggy simulate` with the `docker compose run --rm api npm run cli --` invocation).
  - `backend/API.md`: Rosa two-tier retrieval seam doc (pgvector+Ollama preferred when `corpus_chunk` is populated and reachable via `rosa index`, keyword fallback safe floor; `rosa reset` re-indexes from scratch dev) + `POST /api/v1/treasury/psbt/broadcast` endpoint doc (raw-tx seam first, `sendrawtransaction` → txid; PSBT workflow seam next; bitcoind RPC auth via `BITCOIN_RPC_USER`/`BITCOIN_RPC_PASS`).
  - `docs/DEPLOYMENT.md`: links to `docs/TAILSCALE-ONBOARDING.md`; after-host optional steps (rosa index, rails enable).
  - `docs/ROADMAP.md` + `docs/WORKPLAN.md`: all Phase 3 items marked complete (Rosa retriever, Rosa indexer, Ziggy broadcast + reconcile seam, Ziggy on-chain broadcast plug-in seam, Tailscale self-host + onboarding doc).
  - `.ai_docs/current-status.md` + `docs/KIMI-HANDOFF.md` + `LATEST-UPDATE.md`: refreshed with the full run (two batches) + decisions.
  - New file: `docs/TAILSCALE-ONBOARDING.md` — self-contained, read-only walkthrough any operator can follow to stand up the backend on their own host behind Tailscale. Path A (solo operator, one host, one tailnet) and Path B (any user/team — each operator uses their own Tailscale, ACLs restrict the API port, no shared tailnet, no shared secrets). The onboarding agent sets `.env`, brings up compose, runs migrate + the e2e smoke gate, prints the MagicDNS name + `PUBLIC_API_BASE_URL` value — never touches auth tokens, JWTs, or council data. Opsec notes: `AUTH_SECRET` + `POSTGRES_PASSWORD` generated on the host (`openssl rand -base64 48`), never committed; Tailscale is the access layer (not a public endpoint); Postgres never published to `0.0.0.0`; `.env` stays on the host.
  - Modified: `docs/ROADMAP.md`, `docs/WORKPLAN.md`, `.ai_docs/current-status.md`, `docs/KIMI-HANDOFF.md`, `LATEST-UPDATE.md`.
- **Decisions:** Rosa keeps the keyword fallback as the safe floor (embeddings are an upgrade, not a gate); the broadcast endpoint owns the ledger side of the on-chain leg (real node client returns the txid — today the raw-tx seam is the first real broadcast path, the PSBT workflow seam is the next step); deployment is Tailscale-first and per-user-tailnet (each operator uses their own Tailscale, host joins the operator's tailnet, API at the host's MagicDNS name; no public endpoint, no shared tailnet assumption); the onboarding doc is read-only and never touches auth tokens, JWTs, or council data.
- **Decisions:** Rosa keeps the keyword fallback as the safe floor (embeddings are an upgrade, not a gate); the broadcast endpoint owns the ledger side of the on-chain leg (real node client returns the txid — remaining plug-in); deployment is Tailscale-first and per-user-tailnet (each operator uses their own Tailscale, host joins the operator's tailnet, API at the host's MagicDNS name).
